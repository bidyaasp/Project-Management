# PM Frontend (Vite + React + Tailwind)

Env: add `.env` file with `VITE_API_URL` if backend not at default http://localhost:8000/api

Commands:
- `npm install`
- `npm run dev`

This scaffold connects to the provided FastAPI backend at /api and supports role-based UI (admin/manager/developer).